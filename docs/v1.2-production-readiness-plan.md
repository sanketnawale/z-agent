# Z-Agent v1.2.0 Production Readiness Plan

## Goals
- Improve production deployment safety
- Add health checks
- Improve environment configuration
- Improve Docker Compose
- Add public demo documentation
- Add release checklist
- Prepare for Forge26 and Open Mainframe review

## Non-goals
- No real production customer data
- No destructive automation
- No write actions without approval
- No storing IBM Z credentials permanently

## Release name

`v1.2.0 – Production Readiness Preview`

## Work breakdown

### Friday evening — freeze and plan
- [x] Create clean branch `feat/v1.2-production-readiness` from `main`
- [x] Create this plan
- [x] Commit plan

### Saturday — coding work
- [ ] Add health endpoints
  - FastAPI `GET /api/health` (with `service` and `version`) — keep `/health` for backward compat
  - Django `GET /api/health`
- [ ] Move production settings to environment variables (`settings.py`)
  - `DJANGO_DEBUG`
  - `DJANGO_ALLOWED_HOSTS`
  - `DJANGO_CSRF_TRUSTED_ORIGINS`
  - `DJANGO_SECRET_KEY` (prep, dev default kept)
  - `SECURE_PROXY_SSL_HEADER`
- [ ] Improve Docker Compose
  - `restart: unless-stopped` already present
  - Production environment block
  - `healthcheck` for the web container
- [ ] Expand `.env.example` with safe examples for new env vars
- [ ] Add production deployment doc (`docs/production-deployment.md`)

### Sunday — demo polish and release
- [ ] README screenshots section already exists; add Performance Insights + Safety Settings entries and placeholder images
- [ ] Demo mode foundation
  - `examples/demo-spool-output.txt`
  - `examples/demo-job-summary.json`
  - `docs/demo-mode.md`
- [ ] Test locally (`docker compose up -d --build`, health endpoints)
- [ ] Update `CHANGELOG.md` with the v1.2.0 entry
- [ ] Commit and push, open PR

## Priority order

1. Health endpoints
2. Environment-based settings
3. Docker Compose production cleanup
4. Production deployment doc
5. README screenshots
6. Demo/sample data
7. PR and v1.2.0 release

## What not to do this weekend

- Do not add risky features now
- Do not add automatic job submit/kill actions
- Do not store IBM Z passwords in database
- Do not make AI decide actions directly
- Do not connect public demo to real credentials
- Do not push from old IBM-III folder